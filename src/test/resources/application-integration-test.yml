spring:
  main:
    allow-bean-definition-overriding: true
  data:
    redis:
      password: ""
      database: 0

resi-cache:
  default-ttl: 30m
  early-expiration:
    enabled: false
  sync-lock:
    enabled: true
