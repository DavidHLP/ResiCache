---
phase: "04-test-coverage"
plan: "03"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "src/test/java/io/github/davidhlp/spring/cache/redis/core/writer/chain/BloomFilterFalsePositiveTest.java"
  - "src/test/java/io/github/davidhlp/spring/cache/redis/core/evaluator/SpelConditionEvaluatorTest.java"
autonomous: true
requirements:
  - "TEST-04"
  - "TEST-05"
user_setup: []

must_haves:
  truths:
    - "Bloom filter false positive allows cache to proceed to Redis (not blocking valid requests)"
    - "SpEL expression injection attempts are rejected or handled safely (fail-open)"
  artifacts:
    - path: "src/test/java/io/github/davidhlp/spring/cache/redis/core/writer/chain/BloomFilterFalsePositiveTest.java"
      provides: "Bloom filter false positive behavior verification"
      min_lines: 50
    - path: "src/test/java/io/github/davidhlp/spring/cache/redis/core/evaluator/SpelConditionEvaluatorTest.java"
      provides: "SpEL injection security verification"
      min_lines: 60
  key_links:
    - from: "BloomFilterFalsePositiveTest.java"
      to: "BloomFilterHandler.java"
      via: "Mockito mock BloomSupport"
      pattern: "BloomSupport"
    - from: "SpelConditionEvaluatorTest.java"
      to: "SpelConditionEvaluator.java"
      via: "SpelConditionEvaluator.getInstance()"
      pattern: "shouldProceed"
---

<objective>
Add Bloom Filter false positive behavior tests and SpEL expression injection security tests.

Purpose: TEST-04 verifies chain continues correctly when Bloom says "might contain" but Redis has no value. TEST-05 verifies SpEL injection attacks are blocked or handled safely.
Output: BloomFilterFalsePositiveTest.java, SpelConditionEvaluatorTest.java
</objective>

<context>
@src/main/java/io/github/davidhlp/spring/cache/redis/core/writer/support/protect/bloom/filter/RedisBloomIFilter.java
@src/main/java/io/github/davidhlp/spring/cache/redis/core/evaluator/SpelConditionEvaluator.java
@src/test/java/io/github/davidhlp/spring/cache/redis/core/writer/chain/handler/BloomFilterHandlerTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BloomFilterFalsePositiveTest.java</name>
  <files>src/test/java/io/github/davidhlp/spring/cache/redis/core/writer/chain/BloomFilterFalsePositiveTest.java</files>
  <action>
Create the test file at: src/test/java/io/github/davidhlp/spring/cache/redis/core/writer/chain/BloomFilterFalsePositiveTest.java

This tests Bloom Filter handler chain behavior when Bloom filter returns true (might contain) but Redis actually has no value.

Required test scenarios:
1. bloomSaysMightContainButRedisMiss_continuesToRedis: Mock BloomSupport.mightContain() to return true, but Redis (via chain behavior) returns null - verify chain continues
2. bloomSaysMightContain_chainDoesNotBlockPrematurely: Verify BloomFilterHandler.doHandle() returns result that allows chain to continue when mightContain returns true
3. bloomSaysDefinitelyNotContain_terminatesChain: Verify when mightContain returns false, chain is terminated immediately (existing behavior from BloomFilterHandlerTest)

Create BloomFilterHandler with mocked BloomSupport and CacheStatisticsCollector.

Use the same setUp pattern as BloomFilterHandlerTest.java for consistency.

Note: This tests CHAIN BEHAVIOR, not the Bloom filter's probabilistic false positive rate itself.
</action>
  <verify>
<automated>./mvnw test -Dtest=BloomFilterFalsePositiveTest -q 2>&1 | tail -20</automated>
  </verify>
  <done>BloomFilterFalsePositiveTest.java exists with 3 Bloom chain behavior test scenarios</done>
</task>

<task type="auto">
  <name>Task 2: Create SpelConditionEvaluatorTest.java</name>
  <files>src/test/java/io/github/davidhlp/spring/cache/redis/core/evaluator/SpelConditionEvaluatorTest.java</files>
  <action>
Create the test file at: src/test/java/io/github/davidhlp/spring/cache/redis/core/evaluator/SpelConditionEvaluatorTest.java

Create directory if it does not exist.

This tests SpEL expression injection security. The evaluator uses StandardEvaluationContext with fail-open behavior (catches exceptions and returns safe default).

Required test scenarios:
1. normalExpression_evaluatesCorrectly: Use simple condition like "#args[0] > 0" - verify it evaluates correctly
2. maliciousExpressionRuntimeExec_attackBlocked: Use malicious expression like "T(java.lang.Runtime).getRuntime().exec('ls')" - verify it is either rejected or handled safely (fail-open returns true)
3. maliciousExpressionSystemExit_attackBlocked: Use "T(System).exit(1)" - verify fail-open behavior
4. malformedExpression_failOpen: Use malformed SpEL like "#root.unknown.property" - verify evaluation exception is caught and safe default returned
5. reflectionBasedAttack_attackBlocked: Attempt to access restricted fields/methods via reflection - verify blocked by Spring's security sandbox

Use SpelConditionEvaluator.getInstance() to get the singleton instance.

Create mock CacheableOperation or use reflection to set condition/unless on a test operation.

Key assertion: When malicious expressions are evaluated, the system should NOT throw unhandled exceptions to the caller, and should return a safe default (true for condition = proceed with cache operation).

Import: org.springframework.cache.interceptor.CacheableOperation for creating test operations
</action>
  <verify>
<automated>./mvnw test -Dtest=SpelConditionEvaluatorTest -q 2>&1 | tail -20</automated>
  </verify>
  <done>SpelConditionEvaluatorTest.java exists with 5 security test scenarios</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {user input}->{SpEL evaluator} | Untrusted SpEL expressions |
| {BloomFilter}->{Chain} | Trust Bloom's probabilistic result |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-04-04 | Injection | SpelConditionEvaluator | mitigate | Malicious SpEL blocked by Spring sandbox + fail-open |
| T-04-05 | Tampering | BloomFilter chain | mitigate | False positive tests verify chain continues correctly |
</threat_model>

<verification>
./mvnw test -Dtest=BloomFilterFalsePositiveTest,SpelConditionEvaluatorTest -q
</verification>

<success_criteria>
- BloomFilterFalsePositiveTest.java exists with 3 chain behavior tests
- SpelConditionEvaluatorTest.java exists with 5 security tests
- All tests pass with ./mvnw test -Dtest=BloomFilterFalsePositiveTest,SpelConditionEvaluatorTest
</success_criteria>

<output>
After completion, create `.planning/phases/04-test-coverage/04-03-SUMMARY.md`
</output>
