---
phase: "04-test-coverage"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "pom.xml"
autonomous: true
requirements:
  - "TEST-01"
  - "TEST-02"
user_setup: []

must_haves:
  truths:
    - "TwoListLRU concurrent put/get/evict operations do not corrupt list structure or cause deadlock"
    - "PreRefreshHandler correctly handles race between async refresh and explicit evict/put"
  artifacts:
    - path: "src/test/java/io/github/davidhlp/spring/cache/redis/strategy/eviction/support/TwoListLRUConcurrentTest.java"
      provides: "TwoListLRU concurrency verification"
      min_lines: 80
    - path: "src/test/java/io/github/davidhlp/spring/cache/redis/core/writer/chain/handler/PreRefreshHandlerRaceConditionTest.java"
      provides: "PreRefreshHandler race condition verification"
      min_lines: 60
    - path: "pom.xml"
      provides: "Awaitility dependency"
      contains: "awaitility"
  key_links:
    - from: "TwoListLRUConcurrentTest.java"
      to: "TwoListLRU.java"
      via: "direct instantiation"
      pattern: "new TwoListLRU"
    - from: "PreRefreshHandlerRaceConditionTest.java"
      to: "PreRefreshHandler.java"
      via: "Mockito mocks"
      pattern: "PreRefreshHandler"
---

<objective>
Add concurrency and race condition tests for TwoListLRU and PreRefreshHandler.

Purpose: Verify thread-safety of TwoListLRU under concurrent access and PreRefreshHandler behavior in race scenarios with evict/put operations.
Output: TwoListLRUConcurrentTest.java, PreRefreshHandlerRaceConditionTest.java, Awaitility dependency added
</objective>

<context>
@src/main/java/io/github/davidhlp/spring/cache/redis/strategy/eviction/support/TwoListLRU.java
@src/main/java/io/github/davidhlp/spring/cache/redis/core/writer/chain/handler/PreRefreshHandler.java
@src/test/java/io/github/davidhlp/spring/cache/redis/strategy/eviction/impl/TwoListEvictionStrategyTest.java
@src/test/java/io/github/davidhlp/spring/cache/redis/core/writer/chain/handler/PreRefreshHandlerTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Awaitility dependency to pom.xml</name>
  <files>pom.xml</files>
  <action>
Add the following dependency inside the &lt;dependencies&gt; section of pom.xml (after the existing assertj dependency):

```xml
<dependency>
    <groupId>org.awaitility</groupId>
    <artifactId>awaitility</artifactId>
    <version>4.3.0</version>
    <scope>test</scope>
</dependency>
```

This is required for TEST-01 concurrency tests. Awaitility provides await() semantics for reliable concurrent test execution.
  </action>
  <verify>
<automated>grep -c "awaitility" pom.xml</automated>
  </verify>
  <done>Awaitility 4.3.0 dependency present in pom.xml with test scope</done>
</task>

<task type="auto">
  <name>Task 2: Create TwoListLRUConcurrentTest.java</name>
  <files>src/test/java/io/github/davidhlp/spring/cache/redis/strategy/eviction/support/TwoListLRUConcurrentTest.java</files>
  <action>
Create the test file at: src/test/java/io/github/davidhlp/spring/cache/redis/strategy/eviction/support/TwoListLRUConcurrentTest.java

Create directory if it does not exist.

The test must:
1. Use @ExtendWith(MockitoExtension.class) pattern
2. Create a TwoListLRU instance with small sizes (e.g., maxActive=50, maxInactive=25)
3. Use ExecutorService with 8 threads for concurrent operations
4. Each thread performs 100 put/get operations on distinct keys
5. Use CountDownLatch to coordinate thread start (startLatch) and completion (doneLatch)
6. Use Awaitility.await().atMost(30, TimeUnit.SECONDS).until(() -> doneLatch.getCount() == 0)
7. Track errors in a ConcurrentHashMap or AtomicInteger
8. Verify no data corruption occurred (all put values retrievable)
9. Use @DisplayName annotations for each test scenario

Required test scenarios:
- concurrentPutAndGet_maintainsDataIntegrity: put and get same keys concurrently
- concurrentPutAcrossThreads_noDataCorruption: different threads use different keys
- concurrentEviction_noDeadlock: stress test with rapid put operations

Import pattern from existing tests: org.assertj.core.api.Assertions.assertThat
</action>
  <verify>
<automated>./mvnw test -Dtest=TwoListLRUConcurrentTest -q 2>&1 | tail -20</automated>
  </verify>
  <done>TwoListLRUConcurrentTest.java exists with 3+ concurrent test scenarios using Awaitility</done>
</task>

<task type="auto">
  <name>Task 3: Create PreRefreshHandlerRaceConditionTest.java</name>
  <files>src/test/java/io/github/davidhlp/spring/cache/redis/core/writer/chain/handler/PreRefreshHandlerRaceConditionTest.java</files>
  <action>
Create the test file at: src/test/java/io/github/davidhlp/spring/cache/redis/core/writer/chain/handler/PreRefreshHandlerRaceConditionTest.java

This extends existing PreRefreshHandlerTest.java patterns with race condition scenarios.

Required test scenarios:
1. asyncRefreshAndEvict_concurrentNoCorruption: Simulate async refresh task running while explicit evict happens - verify no NullPointerException or state corruption
2. asyncRefreshAndPut_concurrentCorrectPrecedence: Simulate async refresh task seeing old value while user explicitly puts new value - verify user's explicit put is not lost
3. multipleAsyncRefreshes_onlyLatestWins: Submit multiple async refreshes for same key, verify only the latest one (based on version/timestamp) takes effect

Use Mockito to mock:
- TtlPolicy, PreRefreshSupport, RedisTemplate, CacheStatisticsCollector, ValueOperations

Follow the same setUp pattern as PreRefreshHandlerTest.java (constructor injection via handler = new PreRefreshHandler(...))

Use ExecutorService for async operations and Awaitility.await() for synchronization.
</action>
  <verify>
<automated>./mvnw test -Dtest=PreRefreshHandlerRaceConditionTest -q 2>&1 | tail -20</automated>
  </verify>
  <done>PreRefreshHandlerRaceConditionTest.java exists with 3 race condition test scenarios</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {test thread}->{TwoListLRU} | Concurrent read/write access |
| {PreRefreshHandler}->{RedisTemplate} | Mocked in tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-04-01 | Denial | TwoListLRU | mitigate | Concurrency tests verify no deadlock under load |
| T-04-02 | Denial | PreRefreshHandler | mitigate | Race condition tests verify no state corruption |
</threat_model>

<verification>
./mvnw test -Dtest=TwoListLRUConcurrentTest,PreRefreshHandlerRaceConditionTest -q
</verification>

<success_criteria>
- Awaitility 4.3.0 dependency added to pom.xml
- TwoListLRUConcurrentTest.java runs successfully with 8 concurrent threads
- PreRefreshHandlerRaceConditionTest.java runs successfully with race scenarios
- All tests pass with ./mvnw test -Dtest=TwoListLRUConcurrentTest,PreRefreshHandlerRaceConditionTest
</success_criteria>

<output>
After completion, create `.planning/phases/04-test-coverage/04-01-SUMMARY.md`
</output>
