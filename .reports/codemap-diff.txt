Codemap Diff Report
====================
Generated: 2026-06-21
Trigger: post-refactor "remove dead code and simplify over-engineering" (commit a5ab55b,
         32 files, +42 / -2989, net -2947 lines)

Scope change: 104 main files → 90 main files (14 deleted)

ARCHITECTURE CHANGES SINCE LAST SCAN
------------------------------------
Deleted packages (entire):
  - evaluator/   (SpelConditionEvaluator — condition handled by Spring natively)
  - event/       (CacheEvictedEvent — never published, never listened)
  - wrapper/     (CircuitBreakerCacheWrapper, RateLimiterCacheWrapper — no wiring point)
  - spi/         (BloomFilter, BloomFilterProvider, LockManager, LockProvider, LockHandle,
                 RedissonLockProvider — SPI shell, ServiceLoader never invoked)

Deleted classes (within surviving packages):
  - observability/CacheMetricsRecorder       (zombie bean; recordHit/recordMiss zero callers;
                                              RedisProCache uses MeterRegistry inline)
  - eviction/LockPoolStats                   (test-only DTO)
  - eviction/EvictionStrategyFactory         (@Deprecated; RedisCacheRegister now constructs
                                              TwoListEvictionStrategy directly)
  - protection/bloom/RedisBloomFilterProvider (SPI not wired)

Field/method cleanup:
  - CacheOutput: removed dead lockContext / earlyExpirationDecision / clearSkipRemaining
  - RedisProCacheProperties: removed failOnSpelError (sole reader deleted)

Loading-chain fix:
  - MetricsAutoConfiguration + CachingEnablementValidation now explicitly registered in
    META-INF/spring/...AutoConfiguration.imports (previously relied on @ComponentScan
    side-effect — fragile).

CODEMAPS UPDATED
----------------
architecture.md  - dropped evaluator/wrapper/spi/event package lines + entire SPI section;
                    metadata 104→90 main; CacheOutput fields updated; added explicit auto-config
                    registration note (3 entries in imports file).
backend.md       - removed SPI / evaluator / wrapper / event sections; rewrote Bloom filter as
                    concrete bean wiring (Local/Redis/Hierarchical with @Primary); rewrote Lock
                    as breakdown.LockManager → DistributedLockManager; metrics noted as inline
                    in RedisProCache.
dependencies.md  - replaced "Internal SPI Boundaries" (SPI deleted) with "Extension Points"
                    (BloomIFilter layering via @Primary; LockManager replaceable bean).
data.md          - CacheMetricsRecorder reference → RedisProCache inline MeterRegistry metrics.

DIFF MAGNITUDE
--------------
> 30% (structural: 4 packages + entire SPI system removed). Updated in place per standing
authorization — the refactor is a committed fact; codemaps must reflect the post-refactor
structure. No approval gate needed for doc-only sync.

NEW DEPENDENCIES DETECTED
-------------------------
None. Dependency manifest unchanged by this refactor.

STALENESS WARNINGS
------------------
None. All four codemaps regenerated same day as the refactor they document.
